import { CartProduct } from "./cart.product.model";

export class CartItem{
    public cartItemId:number;
    public cartProduct:CartProduct;
    // constructor(cartId:number,subTotal:number)
    // {
    //     this.cartId=cartId;
    //     this.subTotal=subTotal;
    // }
}